// screens/Chatbot/styles.js

import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1, // Remplit tout l'écran
  },
  webview: {
    flex: 1, // La WebView occupe tout l'espace disponible
  },
});

export default styles;